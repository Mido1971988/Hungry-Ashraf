"use client";
// we can not import Carousel as server Component so we imported here then import this component from CarouselComp (server Component) ... i am not sure if this help performance or not ?
import { Carousel } from "react-responsive-carousel";

export default Carousel;
